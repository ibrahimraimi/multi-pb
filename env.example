# Multi-PB Configuration
# Copy to .env and customize as needed

# External port for accessing all PocketBase instances
# Default is obscure to avoid automated scans
MULTIPB_PORT=25983

# Data directory inside container (usually no need to change)
MULTIPB_DATA_DIR=/var/multipb/data

# Optional: Admin token for API authorization.
# If set, ALL API requests (read and write) require:
#   Authorization: Bearer <token>
# Dashboard: open "Set token" in the sidebar and enter this value.
# MULTIPB_ADMIN_TOKEN=your-secret-token-here
